# Reproductions

Canonical runnable probes currently live under `experiments/` so they can be used by
the learning path. This directory stores candidate-specific raw output or minimized
fixtures when a probe advances toward a patch.

Current links:

- candidate 001: `experiments/03-rules-boundary/`
- candidate 002: `experiments/04-served-content-guard/`
